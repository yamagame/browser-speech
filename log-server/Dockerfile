FROM node:lts-alpine
ARG PORT=4200
ENV PORT $PORT
ARG TARGET_HOST
ARG TARGET_PORT
ENV TARGET_HOST $TARGET_HOST
ENV TARGET_PORT $TARGET_PORT
EXPOSE $PORT
WORKDIR /app
COPY . .
RUN yarn install
CMD ["index.js"]
ENTRYPOINT ["node"]
